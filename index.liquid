extends: default.liquid
---
<div>
  <h2>Fish game development blog</h2>

  <div>
    {% for post in posts %}
      <h4><a href="{{post.path}}">{{ post.title }}</a></h4>
    {% endfor %}
  </div>
</div>
