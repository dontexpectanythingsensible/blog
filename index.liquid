extends: default.liquid
---
<article>
  <h1>Fish game devlog</h1>

  <div>
    {% for post in posts %}
      <h4><a href="{{post.path}}">{{ post.title }}</a></h4>
    {% endfor %}
  </div>
</article>
